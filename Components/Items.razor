@using TodoWarmup.Models
<ul>
  @foreach (var t in Todos)
  {
    <Item Todo="t" OnDelete="OnDelete" />
  }
</ul>

@code
{
  [Parameter]
  public List<TodoItem> Todos { get; set; } = default!;
  [Parameter]
  public EventCallback<Guid> OnDelete { get; set; }
}
